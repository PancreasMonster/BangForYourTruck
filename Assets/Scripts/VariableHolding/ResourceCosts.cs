using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ResourceCosts : MonoBehaviour
{
    public List<string> resourcesID = new List<string>();
    public List<int> resourceCosts = new List<int>();
    // Start is called before the first frame update
    void Start()
    {
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
